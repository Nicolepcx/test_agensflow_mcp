"""Service A — one participant in the event protocol. Implement Component per the CONTRACT."""

from __future__ import annotations

from coordination.protocol import Component


class ServiceA(Component):
    pass  # emit / read / event_id inherited as NotImplementedError until built
