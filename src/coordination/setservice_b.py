"""Set-service B — one participant in the tag-set digest protocol. Implement per the CONTRACT."""

from __future__ import annotations

from coordination.setproto import SetComponent


class SetServiceB(SetComponent):
    pass  # digest inherited as NotImplementedError until built
