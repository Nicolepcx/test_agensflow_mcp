"""taskflow — a synthetic multi-module task manager used as a benchmark
fixture.

Not a real product. The module structure and seeded cross-module bugs
exist so that coding tasks (fix a bug that spans dates + recurrence,
implement a feature touching storage + core + cli, etc.) can be posed
against a known, reproducible codebase with known-correct outcomes.
"""

__version__ = "0.2.0"
